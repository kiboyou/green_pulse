# Dockerfile pour le frontend (optionnel)
FROM node:20-alpine
WORKDIR /app
COPY frontend/package.json /app/package.json
RUN npm install || true
COPY frontend /app
EXPOSE 3000
CMD ["npm", "run", "dev"]
